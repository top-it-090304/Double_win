# root = true: rules from repo root are not inherited — end_of_line lf avoids CRLF + git add warnings on Windows.
root = true

[*]
charset = utf-8
end_of_line = lf
insert_final_newline = true
trim_trailing_whitespace = true

[*.md]
trim_trailing_whitespace = false
